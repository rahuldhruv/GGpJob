
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Grant read/write access to a user's own resume folder
    match /resumes/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
